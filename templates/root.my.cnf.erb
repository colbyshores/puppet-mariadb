[client]
user=root
password="<%= scope.lookupvar('mariadb::real_root_password') %>"

[mysql]
user=root
password="<%= scope.lookupvar('mariadb::real_root_password') %>"

[mysqldump]
user=root
password="<%= scope.lookupvar('mariadb::real_root_password') %>"

[mysqldiff]
user=root
password="<%= scope.lookupvar('mariadb::real_root_password') %>"
